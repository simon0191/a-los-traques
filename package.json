{
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "party:dev": "bunx partykit dev",
    "party:deploy": "bunx partykit deploy",
    "test": "vitest",
    "test:run": "vitest run",
    "test:e2e": "npx playwright test --config tests/e2e/playwright.config.js",
    "test:e2e:headed": "npx playwright test --config tests/e2e/playwright.config.js --headed",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write ."
  },
  "dependencies": {
    "@google/genai": "^1.45.0",
    "@supabase/supabase-js": "^2.100.0",
    "partykit": "^0.0.115",
    "partysocket": "^1.1.16",
    "phaser": "^3.90.0",
    "vite": "^8.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.8",
    "@playwright/test": "^1.58.2",
    "vitest": "^4.1.0"
  }
}
